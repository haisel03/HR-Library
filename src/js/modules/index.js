
// ── Base ─────────────────────────────────────────────
import "./jquery.js";        // 1. jQuery primero (base de Select2, DataTables)
import "./axios.js";         // 2. Axios con interceptores
import "./bootstrap.js";     // 3. Bootstrap componentes

// ── Notificaciones ────────────────────────────────────
import "./swal.js";          // 4. SweetAlert2
import "./notyf.js";         // 5. Notyf (alternativa toast)
import "./toastify.js";      // 6. Toastify (alternativa toast)

// ── Formularios ───────────────────────────────────────
import "./select2.js";       // 7. Select2 (requiere jQuery)
import "./inputmask.js";     // 8. Inputmask con aliases RD
import "./flatpickr.js";     // 9. Flatpickr con locale ES
import "./validation.js";    // 10. Validación nativa
import "./choices.js";			 // 11. Choices.js para lose selects

// ── Tablas ────────────────────────────────────────────
import "./datatables.js";    // 11. DataTables con plugins

// ── Gráficos ──────────────────────────────────────────
import "./chartjs.js";       // 12. Chart.js
import "./vector-maps.js";   // 13. Mapas vectoriales

// ── Calendarios ───────────────────────────────────────
import "./fullcalendar.js";  // 14. FullCalendar con plugins
import "./dayjs.js";         // 15. Day.js para fechas

// ── Editores ──────────────────────────────────────────
import "./quill.js";         // 16. Quill WYSIWYG
import "./signature_pad.js"; // 17. Firma digital

// ── Archivos / Exportación ────────────────────────────
import "./xlsx.js";          // 18. SheetJS Excel

// ── Códigos ───────────────────────────────────────────
import "./codes.js";         // 19. JsBarcode + QRCode

// ── Utilidades ───────────────────────────────────────
import "./dragula.js";       // 20. Dragula drag & drop
import "./simplebar.js";     // 21. SimpleBar scroll personalizado
import "./humanizer.js";     // 22. Humanize duration
import "./feather.js";       // 23. Feather Icons

// ── UI / Template ─────────────────────────────────────

import "./access_control.js";// 26. Control de acceso
