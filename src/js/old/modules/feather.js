// feather-icons
// La inicialización (feather.replace) la hace icons_helper.init()
// que es llamado desde $HR.init() en app.js.
// Este módulo solo expone el global para uso directo desde HTML/demos.
import feather from "feather-icons";

window.feather = feather;
