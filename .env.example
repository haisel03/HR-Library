# ── Servidor de desarrollo ──
# Puerto para el servidor de desarrollo Vite
VITE_DEV_PORT=9000

# ── API ──
# URL base para peticiones API (opcional, se puede definir en App.config)
VITE_API_BASE_URL=http://localhost:3001/api

# ── JSON Server (desarrollo) ──
# Puerto para el servidor mock de datos
JSON_SERVER_PORT=3001
